name: Fixture validation
on: [pull_request, push]
permissions:
  contents: read
jobs:
  fixture-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate fixture
        run: test -s fixtures/status.txt
